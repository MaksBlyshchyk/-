@model IEnumerable<HRReserveSystem.Models.Application>

@{
    ViewData["Title"] = "Заявки";
}

<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-3">
    <div>
        <h1 class="h2 mb-1">Заявки</h1>
        <p class="text-muted mb-0">Етапи відбору кандидатів на конкретні вакансії.</p>
    </div>
    <a class="btn btn-primary" asp-action="Create"><span aria-hidden="true">+</span> Створити заявку</a>
</div>

<form asp-action="Index" method="get" class="card mb-3">
    <div class="card-body row g-3 align-items-end">
        <div class="col-sm-6 col-lg-4">
            <label class="form-label" for="status">Статус відбору</label>
            <select id="status" class="form-select" name="status" asp-items="ViewBag.Statuses">
                <option value="">Усі статуси</option>
            </select>
        </div>
        <div class="col-sm-6 col-lg-3 d-flex gap-2">
            <button class="btn btn-outline-primary flex-fill" type="submit">Фільтрувати</button>
            <a class="btn btn-outline-secondary" asp-action="Index">Скинути</a>
        </div>
    </div>
</form>

<div class="card">
    @if (Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Кандидат</th>
                        <th>Вакансія</th>
                        <th>Статус</th>
                        <th>Дата подачі</th>
                        <th class="text-end">Дії</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    var badgeClass = item.Status switch
                    {
                        "New" => "text-bg-primary",
                        "In review" => "text-bg-info",
                        "Interview" => "text-bg-warning",
                        "Offer" => "text-bg-success",
                        "Accepted" => "text-bg-success",
                        "Hired" => "text-bg-success",
                        "Rejected" => "text-bg-danger",
                        _ => "text-bg-secondary"
                    };

                    <tr>
                        <td>@item.Candidate?.FullName</td>
                        <td>@item.Vacancy?.Title</td>
                        <td><span class="badge @badgeClass">@item.Status</span></td>
                        <td>@item.AppliedAt.ToLocalTime().ToString("g")</td>
                        <td class="text-end text-nowrap">
                            <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><span aria-hidden="true">E</span> Edit</a>
                            <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@item.Id"><span aria-hidden="true">i</span> Details</a>
                            <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id"><span aria-hidden="true">x</span> Delete</a>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            <h2 class="h5">Заявок не знайдено</h2>
            <p class="text-muted mb-3">Змініть фільтр або створіть першу заявку.</p>
            <a class="btn btn-primary" asp-action="Create"><span aria-hidden="true">+</span> Створити заявку</a>
        </div>
    }
</div>
