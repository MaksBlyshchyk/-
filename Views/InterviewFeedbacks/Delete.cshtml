@model InterviewFeedback

@{
    ViewData["Title"] = "Видалити відгук";
}

<h1 class="h2 mb-3">Видалити відгук</h1>
<div class="alert alert-warning">Підтвердіть видалення відгуку для <strong>@Model.Interview?.Application?.Candidate?.FullName</strong>.</div>

<dl class="row">
    <dt class="col-sm-3">Автор</dt>
    <dd class="col-sm-9">@(Model.Recruiter?.FullName ?? "Не вказано")</dd>
    <dt class="col-sm-3">Оцінка</dt>
    <dd class="col-sm-9">@Model.Score/10</dd>
    <dt class="col-sm-3">Рекомендація</dt>
    <dd class="col-sm-9">@Model.Recommendation</dd>
</dl>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Видалити</button>
    <a class="btn btn-outline-secondary" asp-action="Index">Назад</a>
</form>
